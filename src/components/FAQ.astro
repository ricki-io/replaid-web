---
// Neobrutalist FAQ
const faqs = [
  { q: "What channels work?", a: "Instagram, WhatsApp, Messenger, Email, Telegram, Twitter. More coming." },
  { q: "How fast is setup?", a: "Under 10 minutes. Connect, configure, done." },
  { q: "Can I turn off auto-replies?", a: "Yes. Use it just for scoring and notifications if you want." },
  { q: "Is my data safe?", a: "Encrypted. GDPR compliant. We never sell your data." },
];
---

<section id="faq" class="py-16 sm:py-24 bg-brutal-yellow border-t-4 border-brutal-black">
  <div class="mx-auto max-w-4xl px-4 sm:px-6">
    <h2 class="text-3xl sm:text-5xl md:text-6xl font-black uppercase mb-12 sm:mb-16 fade-in-up">
      FAQ
    </h2>
    
    <div class="space-y-4">
      {faqs.map((faq, i) => (
        <details class="card-brutal group fade-in-up" style={`transition-delay: ${i * 50}ms`}>
          <summary class="p-4 sm:p-6 font-black uppercase text-lg sm:text-xl cursor-pointer flex justify-between items-center list-none">
            {faq.q}
            <span class="text-2xl group-open:rotate-45 transition-transform">+</span>
          </summary>
          <div class="px-4 sm:px-6 pb-4 sm:pb-6 text-base sm:text-lg font-medium border-t-4 border-brutal-black pt-4">
            {faq.a}
          </div>
        </details>
      ))}
    </div>
  </div>
</section>
