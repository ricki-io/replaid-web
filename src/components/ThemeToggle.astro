---
// Theme toggle — sun/moon icon, persists to localStorage
---

<button id="theme-toggle" class="theme-toggle" aria-label="Toggle theme" aria-pressed="false">
  <!-- Sun icon (shown in dark mode) -->
  <svg class="theme-icon icon-sun" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
  </svg>
  <!-- Moon icon (shown in light mode) -->
  <svg class="theme-icon icon-moon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
  </svg>
</button>

<style>
  .theme-toggle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.25rem;
    height: 2.25rem;
    border-radius: 0;
    border: 1px solid rgba(var(--overlay), 0.1);
    background: transparent;
    color: rgb(var(--fg));
    cursor: pointer;
    transition: background 0.15s, border-color 0.15s;
  }

  .theme-toggle:hover {
    background: rgba(var(--overlay), 0.06);
    border-color: rgba(var(--overlay), 0.2);
  }

  /* In dark mode: show sun, hide moon */
  :global(.dark) .icon-sun  { display: block; }
  :global(.dark) .icon-moon { display: none; }

  /* In light mode: show moon, hide sun */
  .icon-sun  { display: none; }
  .icon-moon { display: block; }
</style>

<script>
  const btn = document.getElementById('theme-toggle')!;

  // Set initial aria-pressed to match current theme
  btn.setAttribute('aria-pressed', String(document.documentElement.classList.contains('dark')));

  btn.addEventListener('click', () => {
    const isDark = document.documentElement.classList.toggle('dark');
    localStorage.setItem('theme', isDark ? 'dark' : 'light');
    btn.setAttribute('aria-pressed', String(isDark));
    document.getElementById('meta-theme-color')
      ?.setAttribute('content', isDark ? '#000000' : '#fafafa');

    // Notify Three.js scene and other listeners
    window.dispatchEvent(new CustomEvent('theme-change', { detail: { dark: isDark } }));
  });

  // Respond to system preference changes when user hasn't explicitly chosen
  window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', (e) => {
    if (localStorage.getItem('theme')) return; // user has explicit preference
    const dark = e.matches;
    document.documentElement.classList.toggle('dark', dark);
    document.getElementById('meta-theme-color')
      ?.setAttribute('content', dark ? '#000000' : '#fafafa');
    window.dispatchEvent(new CustomEvent('theme-change', { detail: { dark } }));
  });
</script>
