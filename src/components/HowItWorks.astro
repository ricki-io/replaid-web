---
// Dark Mode First How It Works
const steps = [
  { n: '01', title: 'Connect', desc: 'Link your channels in one click.' },
  { n: '02', title: 'Train', desc: 'Add your knowledge and brand voice.' },
  { n: '03', title: 'Launch', desc: 'Watch leads roll in automatically.' },
];
---

<section id="how-it-works" class="py-20 sm:py-32">
  <div class="mx-auto max-w-6xl px-4 sm:px-6">
    <div class="text-center mb-16 fade-in-up">
      <h2 class="text-3xl sm:text-4xl md:text-5xl font-semibold mb-4">How it works</h2>
      <p class="text-lg text-dark-100">Up and running in under 10 minutes.</p>
    </div>
    
    <div class="grid md:grid-cols-3 gap-8">
      {steps.map((s, i) => (
        <div class="text-center fade-in-up" style={`transition-delay: ${i * 100}ms`}>
          <div class="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-electric-500 to-glow-purple rounded-2xl mb-6 shadow-glow">
            <span class="text-xl font-semibold">{s.n}</span>
          </div>
          <h3 class="text-xl font-semibold mb-2">{s.title}</h3>
          <p class="text-dark-100">{s.desc}</p>
        </div>
      ))}
    </div>
  </div>
</section>
