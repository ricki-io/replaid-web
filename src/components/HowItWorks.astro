---
// Japandi How It Works - clean process visualization
const steps = [
  {
    number: '01',
    title: 'Connect',
    description: 'Link your channels in moments. Instagram, WhatsApp, email — they all come together in one peaceful workspace.',
  },
  {
    number: '02',
    title: 'Teach',
    description: 'Share your knowledge and brand voice. The AI learns your way of communicating, your FAQs, your tone.',
  },
  {
    number: '03',
    title: 'Flow',
    description: 'Watch as Replaid handles the routine, surfaces the important, and keeps your pipeline moving. You focus on what matters.',
  },
];
---

<section id="how-it-works" class="section-padding bg-stone-50/50">
  <div class="mx-auto max-w-6xl px-6 lg:px-8">
    <!-- Section header -->
    <div class="text-center max-w-2xl mx-auto mb-20 fade-in-up">
      <div class="flex items-center justify-center gap-3 mb-6">
        <span class="w-8 h-px bg-moss-400"></span>
        <span class="text-sm text-moss-600 tracking-wide uppercase">Process</span>
        <span class="w-8 h-px bg-moss-400"></span>
      </div>
      <h2 class="text-3xl sm:text-4xl lg:text-5xl text-ink-900 mb-6">
        Three simple steps
      </h2>
      <p class="text-lg text-ink-500 leading-relaxed">
        From setup to serenity in under ten minutes.
      </p>
    </div>
    
    <!-- Steps -->
    <div class="grid md:grid-cols-3 gap-12 lg:gap-16">
      {steps.map((step, index) => (
        <div class="relative fade-in-up" style={`transition-delay: ${index * 100}ms`}>
          <!-- Connector line -->
          {index < steps.length - 1 && (
            <div class="hidden md:block absolute top-8 left-full w-full h-px bg-stone-200 -translate-y-1/2" style="width: calc(100% - 2rem); left: calc(100% + 1rem);"></div>
          )}
          
          <div class="text-center">
            <div class="inline-flex items-center justify-center w-16 h-16 bg-white border border-stone-200 rounded-sm mb-6 text-2xl font-serif text-moss-600">
              {step.number}
            </div>
            <h3 class="text-xl font-semibold text-ink-800 mb-3 font-sans">{step.title}</h3>
            <p class="text-ink-500 leading-relaxed">{step.description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
