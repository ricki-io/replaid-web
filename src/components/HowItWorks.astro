---
// Typography First How It Works - numbered steps with big type
const steps = [
  { title: 'Connect', desc: 'Connect your channels with a single click. No API tokens or complex setups.' },
  { title: 'Configure', desc: 'Add brand guidelines, knowledge, and databases to power AI-driven answers.' },
  { title: 'Launch', desc: 'Everything\'s ready — Replaid runs on autopilot, managing replies and leads seamlessly.' },
];
---

<section id="how-it-works" class="py-16 sm:py-24 relative overflow-hidden">
  <!-- Background text -->
  <div class="bg-text text-[25vw] -left-20 top-1/2 -translate-y-1/2">HOW</div>
  
  <div class="relative mx-auto max-w-7xl px-6">
    <div class="mb-20 fade-in-up text-center">
      <div class="mono-label mb-4">Getting Started</div>
      <h2 class="text-display-md font-bold">
        Setup in minutes,<br/>
        <span class="text-accent">runs forever</span>
      </h2>
    </div>
    
    <div class="space-y-0">
      {steps.map((s, i) => (
        <div class="border-t border-white/10 py-12 sm:py-16 flex flex-col items-center text-center gap-6 fade-in-up group" style={`transition-delay: ${i * 100}ms`}>
          <div class="font-display text-5xl sm:text-9xl font-bold text-accent sm:text-ink-500 sm:group-hover:text-accent transition-colors">
            {String(i + 1).padStart(2, '0')}
          </div>
          <div>
            <h3 class="font-display text-2xl sm:text-4xl font-bold mb-3">{s.title}</h3>
            <p class="text-ink-200 text-lg sm:text-xl max-w-lg">{s.desc}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
