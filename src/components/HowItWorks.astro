---
// Art Deco How It Works
const steps = [
  { num: 'I', title: 'Connect', desc: 'Link your channels with a single flourish.' },
  { num: 'II', title: 'Cultivate', desc: 'Impart your knowledge and brand character.' },
  { num: 'III', title: 'Prosper', desc: 'Observe as leads arrive in abundance.' },
];
---

<section id="how-it-works" class="py-24 sm:py-40">
  <div class="mx-auto max-w-6xl px-6">
    <div class="text-center mb-20 fade-in-up">
      <div class="label-deco mb-4">Process</div>
      <h2 class="font-display text-4xl sm:text-5xl md:text-6xl font-bold">
        The <span class="text-gold-gradient italic">Method</span>
      </h2>
      <p class="text-ivory/60 mt-4 text-lg">Operational excellence in mere moments.</p>
    </div>
    
    <div class="grid md:grid-cols-3 gap-8">
      {steps.map((s, i) => (
        <div class="text-center fade-in-up" style={`transition-delay: ${i * 100}ms`}>
          <div class="inline-flex items-center justify-center w-20 h-20 border-2 border-gold mb-8 rotate-45">
            <span class="font-display text-2xl font-bold text-gold -rotate-45">{s.num}</span>
          </div>
          <h3 class="font-display text-2xl font-bold mb-4">{s.title}</h3>
          <p class="text-ivory/60">{s.desc}</p>
        </div>
      ))}
    </div>
  </div>
</section>
