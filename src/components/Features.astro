---
// Neo-Geo Features - bold cards
const features = [
  { color: 'bg-coral', icon: '📥', title: 'Unified Inbox', desc: 'All channels, one beautiful interface.' },
  { color: 'bg-sage', icon: '🎯', title: 'Smart Scoring', desc: 'AI finds your hottest leads.' },
  { color: 'bg-gold', icon: '⚡', title: 'Auto-Replies', desc: 'Lightning responses, always.' },
  { color: 'bg-neo-navy', icon: '🤝', title: 'Human Handoff', desc: 'Seamless escalation when needed.' },
];
---

<section id="features" class="py-24 sm:py-32 relative">
  <!-- Decorative shapes -->
  <div class="absolute top-20 left-10 w-16 h-16 bg-sage/30 rounded-full hidden lg:block"></div>
  <div class="absolute bottom-20 right-20 w-12 h-12 bg-coral/40 hidden lg:block"></div>
  
  <div class="mx-auto max-w-6xl px-6">
    <div class="mb-16 fade-in-up">
      <div class="label-neo mb-4 flex items-center gap-3">
        <span class="w-8 h-1 bg-coral"></span>
        Features
      </div>
      <h2 class="font-display text-4xl sm:text-5xl md:text-6xl font-bold">
        Everything<br/>you need
      </h2>
    </div>
    
    <div class="grid sm:grid-cols-2 gap-6">
      {features.map((f, i) => (
        <div class="card-neo p-8 fade-in-up" style={`transition-delay: ${i * 100}ms`}>
          <div class={`w-14 h-14 ${f.color} flex items-center justify-center mb-6`}>
            <span class="text-2xl">{f.icon}</span>
          </div>
          <h3 class="font-display text-2xl font-bold mb-3">{f.title}</h3>
          <p class="text-neo-black/60">{f.desc}</p>
        </div>
      ))}
    </div>
  </div>
</section>
