---
// Dark Mode First Features
const features = [
  { icon: '📥', title: 'Unified Inbox', desc: 'All messages in one place. No more tab chaos.' },
  { icon: '🎯', title: 'Smart Scoring', desc: 'AI identifies who\'s ready to buy.' },
  { icon: '⚡', title: 'Auto-Replies', desc: 'Respond instantly, even while you sleep.' },
  { icon: '🤝', title: 'Human Handoff', desc: 'AI handles routine. You handle exceptions.' },
];
---

<section id="features" class="py-20 sm:py-32">
  <div class="mx-auto max-w-6xl px-4 sm:px-6">
    <div class="text-center mb-16 fade-in-up">
      <h2 class="text-3xl sm:text-4xl md:text-5xl font-semibold mb-4">
        Everything you need
      </h2>
      <p class="text-lg text-dark-100 max-w-xl mx-auto">
        Powerful features to automate your inbox and convert more leads.
      </p>
    </div>
    
    <div class="grid sm:grid-cols-2 gap-4 sm:gap-6">
      {features.map((f, i) => (
        <div class="card-dark p-6 sm:p-8 fade-in-up transition-all duration-300" style={`transition-delay: ${i * 100}ms`}>
          <div class="text-3xl mb-4">{f.icon}</div>
          <h3 class="text-xl font-semibold mb-2">{f.title}</h3>
          <p class="text-dark-100">{f.desc}</p>
        </div>
      ))}
    </div>
  </div>
</section>
